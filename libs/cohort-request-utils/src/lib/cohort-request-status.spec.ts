import { getRequestStatusFromEvents } from './cohort-request-status';

describe('getRequestStatusFromEvents', () => {
  it('should get latest status from events', () => {
    // TODO add tests
  });
});
