import { sendCohortRequest } from './cohort-request-api-client';

describe('sendCohortRequest', () => {
  it('should work', () => {
    // expect(sendCohortRequest()).toEqual('cohort-request-api-client');
  });
});
